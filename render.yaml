# Render.com Blueprint - DISABLED (manual deploy via Render dashboard)
# To re-enable, uncomment and set DATABASE_URL manually in Render dashboard
# services:
#   - type: web
#     name: telegram-poster-api
#     runtime: python
#     region: frankfurt
#     plan: free
#     rootDir: backend
#     buildCommand: pip install -r requirements.txt
#     startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
#     healthCheckPath: /health
